package net.zcscloud.zhuohcun.zeco.DaoProxy;

public interface GeneralProxy {
    //Not necessary currently, as the fact that different proxy has different methods(implementations). But all the proxies of different types are all proxies.
}
